name: kenari

services:
  gateway:
    image: kenari:latest
    container_name: kenari-gateway
    restart: unless-stopped
    env_file: .env.production
    volumes:
      - gateway-data:/app/data
    depends_on: [kuma, grafana]
    networks: [proxy-net, internal-net]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

  kuma:
    image: louislam/uptime-kuma:1
    container_name: kenari-kuma
    restart: unless-stopped
    volumes:
      - kuma-data:/app/data
    networks: [internal-net]

  grafana:
    image: grafana/grafana:latest
    container_name: kenari-grafana
    restart: unless-stopped
    environment:
      GF_SERVER_ROOT_URL: ${ORIGIN}/grafana
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
      GF_AUTH_PROXY_ENABLED: "true"
      GF_AUTH_PROXY_HEADER_NAME: X-WEBAUTH-USER
      GF_AUTH_PROXY_HEADER_PROPERTY: username
      GF_AUTH_PROXY_AUTO_SIGN_UP: "true"
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD}
    volumes:
      - grafana-data:/var/lib/grafana
    networks: [internal-net]

volumes:
  gateway-data:
  kuma-data:
  grafana-data:

networks:
  proxy-net:
    external: true
    name: nginx-net   # match existing nginx-proxy network
  internal-net:
    driver: bridge
